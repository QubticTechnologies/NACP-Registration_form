# -----------------------------------
# 🌱 LOCAL DEVELOPMENT SETTINGS
# -----------------------------------

# Force local mode (disables Supabase)
LOCAL_DEV=1

# Local PostgreSQL connection
LOCAL_DB_USER=agri_user
LOCAL_DB_PASSWORD=sherline10152
LOCAL_DB_HOST=127.0.0.1
LOCAL_DB_PORT=5432
LOCAL_DB_NAME=agri_census

# Optional — disable SSL for local use
LOCAL_DB_SSLMODE=disable

# -----------------------------------
# (Cloud / Supabase placeholders — ignored when LOCAL_DEV=1)
# -----------------------------------
DB_USER=postgres
DB_PASSWORD=your_supabase_password
DB_HOST=db.example.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_SSLMODE=require
